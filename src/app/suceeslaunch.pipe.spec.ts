import { SuceeslaunchPipe } from './suceeslaunch.pipe';

describe('SuceeslaunchPipe', () => {
  it('create an instance', () => {
    const pipe = new SuceeslaunchPipe();
    expect(pipe).toBeTruthy();
  });
});
