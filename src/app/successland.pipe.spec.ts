import { SuccesslandPipe } from './successland.pipe';

describe('SuccesslandPipe', () => {
  it('create an instance', () => {
    const pipe = new SuccesslandPipe();
    expect(pipe).toBeTruthy();
  });
});
